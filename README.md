# Pairwise-TF-IDF-Knowledge-Extraction
A keyword extraction system that finds words shared meaningfully between two paired documents (e.g. a book synopsis + its author bio), instead of just scoring words within one document like vanilla TF-IDF.
